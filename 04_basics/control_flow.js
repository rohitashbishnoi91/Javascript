//control flow is necessary to make sure that only some part of the code works when a given condition is met

if(true){
    //this code will only be executed when the condition inside the if parenthesis is true.
}

//comparison operators
//<,><=,>=,==,!=,===(checks the type of both operandes).,!==


const balance=1000
//implicit scope only one line of code executed
//eg if() concole.log("hue")

//switch case(redux)

const month=3;

switch(month){
    case 1:
        console.log("jan")
        break
    case 2:
          console.log("feb")
          break;
    default:
        console.log("mismatched")
        break  
}
